package logging;

/**
 * Although this interface is empty, it is used to determine which
 * classes are going to have all their methods logged.
 * @author Song
 *
 */
public interface LogMethods {
	
}
